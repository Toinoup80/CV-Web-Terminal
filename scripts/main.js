const fileSystem = {
    '/': {
        'a-propos-de-moi': {
            'presentation.txt': "Étudiant en 3ème année de BUT Informatique, je recherche un stage de minimum 14 semaines à partir de mars 2025 en informatique. Pour en savoir plus, redirigez vous sur mon compte <a href='https://www.linkedin.com/in/antoine-pisson/' target='_blank'>LinkedIn</a>.",
            'coordonnees.txt': "telephone: 06.50.34.67.72\nemail: antoine.psndh@outlook.fr\nadresse: Amiens, 80000",
            'centres-interets.txt': "Je suis particulièrement passionné par les technologies informatiques, les jeux vidéos, ainsi que les animés et mangas."
        },
        'experiences-professionnelles': {
            'leclerc-drive.txt': "Préparateur de commandes chez Leclerc Drive [Rivery] depuis mars 2022. J'occupe actuellement ce poste le dimanche",
            'soins-service.txt': "J'ai été développeur d’applications chez Soins Service [Boves] du 22 janvier au 15 mars 2024.\nJ'ai développé une application mobile avec Excel, Power Apps et Power Automate pour automatiser la gestion des véhicules empruntés par le personnel soignant.\nJ'ai également mis en place un système de notifications automatisées pour améliorer la communication interne.\nEnfin, j'ai rédigé une documentation technique avec Word pour accompagner l'application et en faciliter la maintenance.",
            'creation-3d-unity.txt': "J'ai conçu, texturé, animé et optimisé des modèles 3D, que j'ai intégré et configuré dans Unity avec C#, pour qu'ils soient réutilisables dans des projets de jeux vidéo. (juillet-août 2024)."
        },
        'formation': {
            'but-informatique.txt': "BUT Informatique à l’IUT d’Amiens, parcours en réalisation d’applications avec spécialisation en conception, développement, et validation.",
            'presentation-but': {
                'introduction.txt': "Le BUT Informatique à l’IUT d’Amiens est une formation complète et progressive, abordant divers domaines essentiels de l’informatique.",
                'algorithmique-et-programmation.txt': "On y développe des compétences en algorithmique avancée pour résoudre des problèmes variés et concevoir des solutions optimisées en plusieurs langages de programmation.",
                'gestion-bases-de-donnees.txt': "On conçoit, modélise et optimise des bases de données pour répondre aux exigences de stockage et de sécurité des données.",
                'reseaux-securite-informatique.txt': "On apprend de nombreuses notions de mise en place et d’administration de réseaux, ainsi que de sécurité informatique, pour garantir la confidentialité et l'intégrité des systèmes d'information.",
                'analyse-et-modelisation.txt': "On utilise des méthodes d’analyse des besoins pour modéliser efficacement les applications et s'assurer de leur adéquation aux attentes des utilisateurs.",
                'developpement-web-et-applicatif.txt': "On manipule de nombreux langages et frameworks pour mettre en place des solutions web et applicatives performantes.",
                'gestion-de-projet.txt': "On emploie des méthodes de gestion de projets, notamment agiles, pour travailler en équipe sur des projets concrets en suivant des processus de développement itératifs.",
                'outils-informatiques-bureautique.txt': "On utilise des outils informatiques de bureautique et de la suite Office 365 pour optimiser la productivité et automatiser les processus dans divers projets."
            },
            'bac.txt': "J'ai obtenu le Baccalauréat Général au Lycée Madeleine Michelis à Amiens, en ayant pris les spécialités mathématiques et NSI (Numérique et sciences informatiques), où j'ai obtenu la mention Bien."
        },
        'competences': {
            'developpement-web.txt': "HTML, CSS, PHP, MySQL, JavaScript, Bootstrap, WordPress, Node.js, React.js, Vue.js, Leaflet.js.",
            'developpement-logiciel-et-jeux.txt': "C++, C#, Java, Python, Unity.",
            'gestion-bases-de-donnees.txt': "Gestion des bases de données, création de structures optimisées pour le stockage et l'analyse.",
            'outils-bureautiques.txt': "Suite Office 365, niveau avancé en Excel, Word, PowerPoint, Power Apps et Power Automate.",
            'conception-graphique-sonore-3d.txt': "Modélisation 3D, création sonore et graphique, maîtrise de Blender, Canva et Audacity.",
            'intelligence-artificielle.txt': "Bases en développement d'IA pour jeux simples et intégration de scripts intelligents."
        },
        'outils-informatiques': {
            'outils-utilises.txt': "Utilisés pour projets de développement, simulation réseau, conteneurisation, modélisation de données et gestion de version.",
            'liste.txt': "Voici une partie des outils informatiques que j'ai utilisé afin de mener à bien mes projets :\nTrello, Asana, Cisco Packet Tracer, Visual Studio, Visual Studio Code, IntelliJ, Blender, Audacity, Unity, GitHub, WordPress, Docker, Looping, Canva, Excel, Word, PowerPoint, Power Apps, Power Automate"
        },
        'atouts-personnels': {
            'qualites-personnelles.txt': "Polyvalent, autonome, adaptable, habitué au travail en équipe avec une bonne capacité à résoudre des problèmes et une aptitude à l'autoformation.",
            'valeur-ajoutee.txt': "Grâce à la diversité de mes compétences et connaissances, je suis convaincu de pouvoir apporter une réelle valeur ajoutée à votre entreprise.",
        },
        'lettre-de-motivation.txt': `🔔 Étudiant en BUT Informatique – À la recherche d’un stage 🔔

Madame, Monsieur,

Actuellement étudiant en troisième année de BUT Informatique à l’IUT d’Amiens, je recherche un stage de 14 semaines ou plus, à partir de mars 2025, pour mettre mes compétences techniques au service de projets concrets. Ce stage me permettra de contribuer au développement et à l’optimisation de solutions digitales tout en consolidant mon expérience.\n
👉 Comment puis-je intervenir dans votre entreprise ?

Durant ce stage, je pourrais :

- Développer des solutions web et applicatives : Grâce à ma maîtrise de nombreux outils et langages informatiques, je suis capable de concevoir un site web ou une application de A à Z, en m’assurant qu’elle réponde parfaitement aux attentes des utilisateurs.

- Optimiser et automatiser les processus bureautiques : J’ai acquis une solide expérience sur la suite Office 365, notamment Excel et Word. Je pourrais ainsi créer des outils sur mesure pour automatiser et simplifier les tâches récurrentes, contribuant à accroître la productivité et à réduire les erreurs.

- Gérer et structurer les bases de données : Je suis en mesure de gérer les données de façon efficace en mettant en place des structures sécurisées et optimisées pour répondre aux besoins de stockage et d’analyse.

- Créer des contenus visuels, 3D et sonores : Mon expérience en modélisation 3D, en création graphique et sonore, ainsi que ma maîtrise des outils bureautiques, me permettent de produire des éléments visuels variés pour vos projets. Je peux concevoir des schémas, diagrammes, présentations et visuels interactifs pour mieux communiquer les informations et renforcer l’attractivité de vos projets.

- Fournir un support technique et résoudre les problématiques matérielles et logicielles : Grâce à ma bonne compréhension des composants informatiques, de l'architecture des systèmes, et des environnements logiciels internes, je suis en mesure d’intervenir aussi bien sur des aspects matériels que logiciels, en accompagnant les équipes dans la gestion, le dépannage et l’optimisation de leur environnement informatique.

👉 Quels sont mes principaux atouts ?

Polyvalent, autonome, adaptable et habitué au travail en équipe, je suis convaincu de pouvoir apporter une réelle valeur ajoutée à votre entreprise.

👉 Possibilité de CDI :

À l’issue de ce stage, je suis également ouvert à l’opportunité d’un CDI, afin de m’investir durablement dans votre entreprise et de contribuer activement à son développement.

👉 Me contacter :

Retrouvez plus d'informations sur mon compte <a href='https://www.linkedin.com/in/antoine-pisson/' target='_blank'>LinkedIn</a>.

Antoine Pisson
Étudiant en 3ème année de BUT Informatique
📍 Amiens, 80000
📞 06.50.34.67.72
📩 antoine.psndh@outlook.fr`
    }
};



let currentPath = '/';  // Chemin actuel
const input = document.getElementById('command-input');
const terminal = document.getElementById('terminal');
let messageAffiche = false;  // Pour s'assurer que le message est affiché une seule fois

// Message de bienvenue
function displayWelcomeMessage() {
    if (!messageAffiche) {
        const messageNode = document.createElement('div');
        messageNode.textContent = "Bienvenue dans mon espace interactif ! Je suis Antoine Pisson, étudiant en informatique, et actuellement en recherche d'un stage de 14 semaines ou plus pour 2025. Tapez 'help' pour en savoir plus sur moi et mes compétences.";
        terminal.insertBefore(messageNode, input.parentElement);
        messageAffiche = true;
    }
}

// Mettre à jour le prompt avec le chemin actuel et l'heure
function updatePrompt() {
    const inputLine = document.querySelector('.input-line');
    const prompt = inputLine.querySelector('.prompt');
    const time = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
    prompt.textContent = `user@linux:${currentPath} [${time}] $`;
    input.focus();  // S'assurer que l'input reste focus
}

// Afficher un message dans le terminal avec animation fadeIn
function displayMessage(message, className = '') {
    const messageNode = document.createElement('div');
    messageNode.innerHTML = message;
    if (className) {
        messageNode.classList.add(className);
    }
    messageNode.classList.add('fadeIn'); 
    terminal.insertBefore(messageNode, input.parentElement);
}

// Gestion des commandes
input.addEventListener('keydown', function (event) {
    if (event.key === 'Enter') {
        const command = input.value.trim().toLowerCase();
        displayMessage(`user@linux:${currentPath} $ ${command}`);  // Affiche la commande entrée

        if (command === 'ls') {
            listDirectory();
        } else if (command.startsWith('cd')) {
            const dir = command.split(' ')[1];
            changeDirectory(dir);
        } else if (command.startsWith('cat')) {
            const file = command.split(' ')[1];
            readFile(file);
        } else if (command === 'help') {
            displayHelp();
        } else if (command === 'clear') {
            clearTerminal();
        } else {
            displayMessage(`Commande non reconnue: ${command}`);
        }

        input.value = '';  
        updatePrompt();  
        terminal.scrollTop = terminal.scrollHeight;  // Garde le terminal en bas après chaque commande
    }
});

// Fonction pour lister les répertoires
function listDirectory() {
    const currentDir = getDirectory(currentPath);
    const items = Object.keys(currentDir).map(item => {
        if (typeof currentDir[item] === 'object') {
            return `<span class="directory">${item}/</span>`;  // Style pour les dossiers
        } else {
            return `<span class="file">${item}</span>`;  // Style pour les fichiers
        }
    }).join('  ');

    displayMessage(items);
}


// Fonction pour changer de répertoire
function changeDirectory(dir) {
    if (!dir) {
        displayMessage(`cd: chemin manquant`);
        return;
    }

    let newPath;

    if (dir === '/') {
        // Commande pour revenir directement à la racine
        currentPath = '/';
        updatePrompt();
        return;
    }

    if (dir.startsWith('/')) {
        // Chemin absolu
        newPath = dir.endsWith('/') ? dir : dir + '/';
    } else {
        // Chemin relatif au chemin actuel
        newPath = currentPath + (currentPath === '/' ? '' : '/') + dir;
    }

    // Normaliser le chemin en gérant les '..' et '.'
    const pathSegments = newPath.split('/').filter(Boolean);
    const resolvedPathSegments = [];

    for (let segment of pathSegments) {
        if (segment === '..') {
            if (resolvedPathSegments.length > 0) resolvedPathSegments.pop();
        } else if (segment !== '.') {
            resolvedPathSegments.push(segment);
        }
    }

    // Reconstruire le chemin final en tenant compte de la racine
    const finalPath = '/' + resolvedPathSegments.join('/') + (resolvedPathSegments.length > 0 ? '/' : '');

    // Vérifier si le chemin existe dans le fileSystem
    const targetDir = getDirectory(finalPath);
    if (typeof targetDir === 'object') {
        currentPath = finalPath;
    } else {
        displayMessage(`cd: ${dir}: Aucun dossier de ce type`);
    }
}


// Fonction pour lire un fichier
function readFile(file) {
    const currentDir = getDirectory(currentPath);

    // Vérifie le nom du fichier avec et sans extension .txt
    if (currentDir[file]) {
        if (typeof currentDir[file] === 'object') {
            displayMessage(`cat: ${file}: est un répertoire`);
        } else {
            displayMessage(currentDir[file], 'console-response');
        }
    } else if (currentDir[`${file}.txt`]) {
        if (typeof currentDir[`${file}.txt`] === 'object') {
            displayMessage(`cat: ${file}.txt: est un répertoire`);
        } else {
            displayMessage(currentDir[`${file}.txt`], 'console-response');
        }
    } else {
        displayMessage(`cat: ${file}: Aucun fichier de ce type`);
    }
}



// Fonction pour obtenir le répertoire courant
function getDirectory(path) {
    const parts = path.split('/').filter(Boolean);
    let currentDir = fileSystem['/'];
    for (let part of parts) {
        currentDir = currentDir[part];
    }
    return currentDir;
}

// Fonction pour nettoyer le terminal
function clearTerminal() {
    terminal.innerHTML = '';  // Vide tout le terminal
    terminal.appendChild(input.parentElement);  // Garde seulement la ligne de commande

    // Ajout d'une marge supplémentaire après un clear
    const marginNode = document.createElement('div');
    marginNode.style.height = '28px';  // Espace de entre le haut et la ligne de commande
    terminal.insertBefore(marginNode, input.parentElement);

    updatePrompt();
}

// Afficher l'aide avec alignement des :
function displayHelp() {
    const helpMessage = `
Commandes disponibles:
ls                : Lister le contenu du répertoire courant
cd &lt;répertoire&gt;   : Changer de répertoire
cd ..             : Revenir au répertoire précédent
cd /              : Revenir directement à la racine
cat &lt;fichier&gt;     : Lire un fichier
clear             : Nettoyer l'écran du terminal
help              : Afficher l'aide
    `;
    displayMessage(helpMessage);
}



// Initialisation du message de bienvenue et du prompt
displayWelcomeMessage();
updatePrompt();
